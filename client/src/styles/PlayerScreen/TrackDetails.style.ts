import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        width: "100%",
        marginVertical: 30,
        justifyContent: "space-between",
        alignItems: "center",
    },

    textTitle: {
        fontWeight: "bold",
        fontSize: 16,
    },
});
